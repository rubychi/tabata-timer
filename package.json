{
	"scripts": {
		"postinstall": "lerna bootstrap && lerna run build && mv ./packages/client/googled94ac434ac0fe50e.html ./build",
		"start": "node packages/server/index.js"
	},
	"engines": {
    "node": "8.1.3"
  },
	"devDependencies": {
		"lerna": "^2.0.0"
	}
}
