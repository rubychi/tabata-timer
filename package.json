{
	"scripts": {
		"postinstall": "lerna bootstrap && lerna run build",
		"start": "node packages/server/index.js"
	},
	"engines": {
    "node": "8.1.3"
  },
	"devDependencies": {
		"lerna": "^2.0.0"
	}
}
